<tr class="text-{{ videresendt.innslag > 4 ? 'error':'success'}}">
	<td></td>
	<td>4 innslag</td>
	<td>{{ videresendt.innslag }} innslag</td>
	<td>{{ videresendt.innslag > 4 ? 'Over kvote!':'OK' }}</td>
</tr>
<tr class="text-{{ videresendt.personer > (videresendt.innslag * 2) ? 'error':'success'}}">
	<td></td>
	<td>{{ videresendt.innslag * 2 }} person{{ (videresendt.innslag * 2) != 1 ? 'er' }}</td>
	<td>{{ videresendt.personer }} person{{ videresendt.personer != 1 ? 'er' }}</td>
	<td>{{ videresendt.personer > (videresendt.innslag * 2) ? 'Over kvote!':'OK' }}</td>
</tr>
{% if videresendt.mangler_personer %}
	<tr class="text-danger">
		<td></td>
		<td colspan="3">
			OBS: Minst ett av utstilling-innslagene er sendt videre uten at det følger med noen personer.
			<br />
			Dette er ikke mulig, og vil føre til at innslaget blir meldt av UKM-festivalen.
		</td>
	</tr>
{% endif %}

<tr>
	<td></td>
	<td colspan="3" class="small">
		En bilde-serie regnes som ett verk, men må da være påmeldt som flere titler i innslaget.
	</td>
</tr>
