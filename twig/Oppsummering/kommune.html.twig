{% set videre = summering[ monstring.getId() ] %}

<h3 class="mt-3">Videresendt til {{ monstring.getNavn() }}</h3>
<table class="table table-striped table-inverse">
	<tbody>
		<tr>
			<th>Scene</th>
			<th>{{ videre.scene.personer > 0 ? videre.scene.personer : 0 }} personer</th>
			<th>{{ videre.scene.innslag > 0 ? videre.scene.innslag : 0 }} innslag</th>
			<th>{{ videre.scene.varighet > 0 ? videre.scene.varighet|varighet : 0 }}</th>
		</tr>
		<tr>
			<td> &nbsp; Musikk</td>
			<td>{{ videre.musikk.personer > 0 ? videre.musikk.personer : 0 }} personer</td>
			<td>{{ videre.musikk.innslag > 0 ? videre.musikk.innslag : 0 }} innslag</td>
			<td>{{ videre.musikk.varighet > 0 ? videre.musikk.varighet|varighet : 0 }}</td>
		</tr>
		<tr>
			<td> &nbsp; Dans</td>
			<td>{{ videre.dans.personer > 0 ? videre.dans.personer : 0 }} personer</td>
			<td>{{ videre.dans.innslag > 0 ? videre.dans.innslag : 0 }} innslag</td>
			<td>{{ videre.dans.varighet > 0 ? videre.dans.varighet|varighet : 0}}</td>
		</tr>
		<tr>
			<td> &nbsp; Teater</td>
			<td>{{ videre.teater.personer > 0 ? videre.teater.personer : 0 }} personer</td>
			<td>{{ videre.teater.innslag > 0 ? videre.teater.innslag : 0 }} innslag</td>
			<td>{{ videre.teater.varighet > 0 ? videre.teater.varighet|varighet : 0 }}</td>
		</tr>
		<tr>
			<td> &nbsp; Litteratur</td>
			<td>{{ videre.litteratur.personer > 0 ? videre.litteratur.personer : 0 }} personer</td>
			<td>{{ videre.litteratur.innslag > 0 ? videre.litteratur.innslag : 0 }} innslag</td>
			<td>{{ videre.litteratur.varighet > 0 ? videre.litteratur.varighet|varighet :0 }}</td>
		</tr>
		<tr>
			<td> &nbsp; Annet på scene</td>
			<td>{{ videre.annet.personer > 0 ? videre.annet.personer : 0 }} personer</td>
			<td>{{ videre.annet.innslag > 0 ? videre.annet.innslag : 0 }} innslag</td>
			<td>{{ videre.annet.varighet > 0 ? videre.annet.varighet|varighet :0 }}</td>
		</tr>

		<tr>
			<th>Film</th>
			<td>{{ videre.video.personer > 0 ? videre.video.personer : 0 }} personer</td>
			<td>{{ videre.video.innslag > 0 ? videre.video.innslag : 0 }} innslag</td>
			<td>{{ videre.video.varighet > 0 ? videre.video.varighet|varighet : 0 }}</td>
		</tr>
		<tr>
			<th>Utstilling</th>
			<td>{{ videre.utstilling.personer > 0 ? videre.utstilling.personer : 0 }} personer</td>
			<td>{{ videre.utstilling.innslag > 0 ? videre.utstilling.innslag : 0 }} innslag</td>
			<td>{{ videre.utstilling.titler  > 0 ? videre.utstilling.titler : 0 }} kunstverk</td>
		</tr>

		<tr>
			<th>Arrangører</th>
			<td>{{ videre.arrangor.personer > 0 ? videre.arrangor.personer : 0 }} personer</td>
			<td></td>
			<td></td>
		</tr>
		<tr>
			<th>Konferansierer</th>
			<td>{{ videre.konferansier.personer > 0 ? videre.konferansier.personer : 0 }} personer</td>
			<td></td>
			<td></td>
		</tr>
		<tr>
			<th>UKM Media</th>
			<td>{{ videre.nettredaksjon.personer > 0 ? videre.nettredaksjon.personer : 0 }} personer</td>
			<td></td>
			<td></td>
		</tr>
	</tbody>
	
	<tfoot>
		<tr>
			<th>TOTALT</th>
			<th>
				{{ videre.total.personer > 0 ? videre.total.personer : 0 }} personer
				<br />
				{{ videre.total.unike > 0 ? videre.total.unike : 0 }} unike personer
			</th>
			<th>
				{{ videre.total.innslag > 0 ? videre.total.innslag : 0 }} innslag
			</th>
			<th>
				{{ videre.total.varighet > 0 ? videre.total.varighet|varighet : 0 }}
			</th>
		</tr>
	</tfoot>
</table>

<p>&nbsp;</p>