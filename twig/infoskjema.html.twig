{% extends "Layout/base.html.twig" %}

{% block content %}
<form method="POST" action="?page={{GET('page')}}&til={{GET('til')}}&action=infoskjema">
	{% include "Skjema/innhold.html.twig" with {arrangement: til.getArrangement(), skjema: til.getArrangement().getSkjema()} %}

    <button type="submit" class="btn btn-ukm btn-success">Lagre</button>
</form>
{% endblock %}